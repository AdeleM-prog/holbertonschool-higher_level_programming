#!/usr/bin/python3
""" no module imported"""


def is_same_class(self, a_class):
    """a function that returns True if the object
    is exactly an instance of the specified class;
    otherwise False"""
    return type(self) == a_class
